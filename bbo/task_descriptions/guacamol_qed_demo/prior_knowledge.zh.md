# 领域先验知识

- 对有效分子来说，GuacaMol 风格的 QED 分数落在 `[0, 1]` 区间内。
- 这个任务通过报告 `1.0 - score` 将该分数转换为最小化目标。
- 随包提供的候选池经过整理，旨在包含有效候选；但如果某次出现无效分子，实现会遵循 GuacaMol 对损坏分数的约定，将其视为 `-1.0`，对应的 loss 为 `2.0`。
- 由于候选池是固定且本地的，搜索质量完全取决于提出了哪些候选池成员，而不取决于生成式化学能力。
