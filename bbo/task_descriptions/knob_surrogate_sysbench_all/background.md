# Background

Sysbench full-knob surrogate (196 dims). Offline sklearn RF from KnobsTuningEA ``tuning_benchmark/surrogate/``.
Full MySQL knob vector (196 active dimensions). Copy ``SYSBENCH_all.joblib``.
